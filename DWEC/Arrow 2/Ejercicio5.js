// Pasar de decimal a binario.
const decimalbinary = (decimal) => decimal.toString(2);

const decimal = 13;
const binary = decimalbinary(decimal);

console.log("Ese numero decimal: " + decimal + "en numero binarrio es: " + binary );
