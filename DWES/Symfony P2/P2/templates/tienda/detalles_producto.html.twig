{# templates/tienda/detalles_producto.html.twig #}

<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Detalles del Producto - {{ producto.name }}</title>
    <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
</head>
<body>
    <h1>{{ producto.name }}</h1>
    <p>{{ producto.description }}</p>
    <p>Precio: {{ producto.price }}</p>
    {# Puedes agregar más detalles aquí, como imágenes, especificaciones, etc. #}
    <a href="{{ path('app_tienda') }}">Volver a la lista de productos</a>
</body>
</html>
